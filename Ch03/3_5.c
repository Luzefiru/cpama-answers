/*
I predict the answer to be:
12.3 45 0.600000

[✓]
*/

#include <stdio.h>

int main(void)
{
    int i;
    float x, y;

    scanf("%f%d%f", &x, &i, &y);
    printf("%f %d %f", x, i, y);

    return 0;
}